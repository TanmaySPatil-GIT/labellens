# PostgreSQL connection URL (Neon or Local PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/labellens

# Port to run the FastAPI server on (defaults to 8000 if not set)
PORT=8000

# Google Gemini API Key (obtain from Google AI Studio)
GEMINI_API_KEY=your_gemini_api_key_here
